{
  "rules": {
    "onlineRoomStates": {
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },
    "tvDisplaySessions": {
      "$sessionCode": {
        ".read": true,
        ".write": true
      }
    },
    "sponsorAds": {
      ".read": true,
      ".write": "auth != null && (auth.token.isAdmin == true || auth.token.isSuperAdmin == true || auth.token.role == 'admin' || auth.token.role == 'super_admin')"
    }
  }
}